# -----------------------------------------------
# local_ai.py
# -----------------------------------------------
# This file is meant for local development and testing only.
# 
# Purpose:
# - Write and debug your AI logic (e.g., summarization, classification, etc.)
# - Test functionality by running this script directly in the terminal
#
# Guidelines:
# - Do NOT use file paths to read data; use function arguments instead
# - Keep your functions pure, testable, and reusable
# - from data_format import test_cases
#
# -for case in test_cases:
#     func_name = case['function']
#     args = case['input']
#     # call function dynamically or via if-else and pass args
#
# Example Usage:
#   python local_ai.py
#
# Once the code works correctly, you can:
# - Move the logic into a separate function (if not already)
# - Pass data as arguments (dict/list/json), not via request.data or files
# -----------------------------------------------
